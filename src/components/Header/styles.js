import { StyleSheet } from 'react-360'

const styles = StyleSheet.create({
  header: {
    height: 400,
  },
  headerTitle: {
    fontSize: 60,
    textAlign: 'center',
    color: '#fff'
  },
  headerSubtitle: {
    fontSize: 30,
    textAlign: 'center',
    color: '#fff'
  }
});

export default styles;